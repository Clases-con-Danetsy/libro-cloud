---
import Layout from "../layouts/layout.astro";
import Menu from "../components/menu.astro";
import Sidebar from "../components/sidebar.astro";
import Button from "@/components/button.astro";
---

<Layout title="Nuevo Rol">
  <Menu />
  <div class="flex flex-row gap-4">
    <Sidebar />
    <div class="flex-1 p-6">
      <div class="max-w-2xl">
        <h1 class="text-2xl font-bold mb-6">Crear Nuevo Rol</h1>

        <form id="formRol" class="space-y-4">
          <div>
            <label for="rol_name" class="block text-sm font-medium mb-2">
              Nombre del Rol <span class="text-red-500">*</span>
            </label>
            <input
              type="text"
              id="rol_name"
              name="rol_name"
              required
              class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="Ej: Administrador, Usuario, Editor..."
            />
          </div>

          <div
            id="errorMessage"
            class="hidden bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded"
          >
          </div>

          <div
            id="successMessage"
            class="hidden bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded"
          >
          </div>

          <div class="flex gap-3">
            <Button
              type="submit"
              variant="default"
              size="default"
              id="btnGuardar"
            >
              Crear Rol
            </Button>
            <a href="/roles">
              <Button type="button" variant="outline" size="default">
                Cancelar
              </Button>
            </a>
          </div>
        </form>
      </div>
    </div>
  </div>
</Layout>

<script>
  import { navigate } from "astro:transitions/client";
  const userStr = localStorage.getItem("user");
  const user = userStr ? JSON.parse(userStr) : null;
  const form = document.getElementById("formRol") as HTMLFormElement;
  const errorMessage = document.getElementById("errorMessage");
  const successMessage = document.getElementById("successMessage");
  const btnGuardar = document.getElementById("btnGuardar");

  form?.addEventListener("submit", async (e) => {
    e.preventDefault();

    // Limpiar mensajes previos
    errorMessage?.classList.add("hidden");
    successMessage?.classList.add("hidden");

    const formData = new FormData(form);
    const rolName = formData.get("rol_name") as string;

    if (!rolName || rolName.trim() === "") {
      if (errorMessage) {
        errorMessage.textContent = "El nombre del rol es obligatorio";
        errorMessage.classList.remove("hidden");
      }
      return;
    }

    try {
      // Deshabilitar botón mientras se envía
      if (btnGuardar) {
        btnGuardar.setAttribute("disabled", "true");
        btnGuardar.textContent = "Guardando...";
      }

      const response = await fetch(
        `http://127.0.0.1:8000/rol/create?rol_name=${encodeURIComponent(rolName)}&who_create=${user.id}&who_update=${user.id}`,
        {
          method: "POST",
          headers: {
            accept: "application/json",
          },
        },
      );

      const data = await response.json();

      if (response.ok) {
        if (successMessage) {
          successMessage.textContent =
            data.mensaje || "Rol creado correctamente";
          successMessage.classList.remove("hidden");
        }

        setTimeout(() => {
          navigate("/roles");
        }, 1500);
      } else {
        throw new Error(data.detail || "Error al crear el rol");
      }
    } catch (error) {
      console.error("Error:", error);
      if (errorMessage) {
        errorMessage.textContent =
          error instanceof Error ? error.message : "Error al crear el rol";
        errorMessage.classList.remove("hidden");
      }

      if (btnGuardar) {
        btnGuardar.removeAttribute("disabled");
        btnGuardar.textContent = "Crear Rol";
      }
    }
  });
</script>
